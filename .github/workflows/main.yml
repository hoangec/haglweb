name: 🚀 Deploy to web server
on:
    - push
    - workflow_dispatch
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: haglgroup.vn
        username: hagl
        password: ${{ secrets.ftp_password }}
        local-dir: ./octobercms-files/
        server-dir: ./octobercms-files/
        port: 2121
